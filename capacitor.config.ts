import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.glo',
  appName: 'glo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
